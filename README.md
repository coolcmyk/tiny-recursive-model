# tiny-recursive-model
an attempt to implement https://arxiv.org/abs/2510.04871?ref=refetch.io

activate the environment:

```bash
# On Windows
.\venv\Scripts\activate
```
install dependencies:

```bash
pip install -r requirements.txt
```